{
  "$schema": "https://json.schemastore.org/lerna.json",
  "version": "independent",
  "npmClient": "npm",
  "stream": true,
  "ignoreChanges": ["*.md"],
  "packages": ["packages/*"],
  "command": {
    "run": { "parallel": true }
  }
}
